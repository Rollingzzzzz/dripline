"""dripline.ext — optional framework integrations.

These modules import third-party frameworks and are never imported by the
dripline core; install the matching extra (``pip install 'dripline[fastapi]'``).
"""
